plugins {
    id 'java-library'
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id 'net.neoforged.moddev' version '2.0.141'
}

tasks.named('wrapper', Wrapper).configure {
    // Define wrapper values here so as to not have to always do so when updating gradlew.properties.
    // Switching this to Wrapper.DistributionType.ALL will download the full gradle sources that comes with
    // documentation attached on cursor hover of gradle classes and methods. However, this comes with increased
    // file size for Gradle. If you do switch this to ALL, run the Gradle wrapper task twice afterwards.
    // (Verify by checking gradle/wrapper/gradle-wrapper.properties to see if distributionUrl now points to `-all`)
    distributionType = Wrapper.DistributionType.BIN
}

version = mod_version
group = mod_group_id

repositories {
    mavenLocal()
    maven {
        url "https://www.cursemaven.com"
    }
    maven {
        url 'https://maven.blamejared.com'
    }
    flatDir {
        dir 'libs'
    }
    maven {
        name 'LDTTeam - Modding'
        url 'https://ldtteam.jfrog.io/ldtteam/modding/'
    }
}

base {
    archivesName = mod_id
}

// Mojang ships Java 25 to end users starting in 26.1, so mods should target Java 25.
java.toolchain.languageVersion = JavaLanguageVersion.of(25)

neoForge {
    // Specify the version of NeoForge to use.
    version = project.neo_version

    // This line is optional. Access Transformers are automatically detected
    accessTransformers = project.files('src/main/resources/META-INF/accesstransformer.cfg')

    // Default run configurations.
    // These can be tweaked, removed, or duplicated as needed.
    runs {
        client {
            client()

            // Comma-separated list of namespaces to load gametests from. Empty = all namespaces.
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }

        server {
            server()
            programArgument '--nogui'
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }

        // This run config launches GameTestServer and runs all registered gametests, then exits.
        // By default, the server will crash when no gametests are provided.
        // The gametest system is also enabled by default for other run configs under the /test command.
        gameTestServer {
            type = "gameTestServer"
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }
        data {
            clientData()
            programArguments.addAll '--mod', project.mod_id, '--all', '--output', file('src/generated/resources/').getAbsolutePath(), '--existing', file('src/main/resources/').getAbsolutePath()
        }

        // applies to all the run configs above
        configureEach {
            // Recommended logging data for a userdev environment
            // The markers can be added/remove as needed separated by commas.
            // "SCAN": For mods scan.
            // "REGISTRIES": For firing of registry events.
            // "REGISTRYDUMP": For getting the contents of all registries.
            systemProperty 'forge.logging.markers', 'REGISTRIES'

            // Recommended logging level for the console
            // You can set various levels here.
            // Please read: https://stackoverflow.com/questions/2031163/when-to-use-the-different-log-levels
            logLevel = org.slf4j.event.Level.DEBUG
        }
    }

    mods {
        // define mod <-> source bindings
        // these are used to tell the game which sources are for which mod
        // mostly optional in a single mod project
        // but multi mod projects should define one per mod
        "${mod_id}" {
            sourceSet(sourceSets.main)
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources { srcDir 'src/generated/resources' }

// Mod-compat layer: enable per-module as the upstream mod publishes a 26.1.2 build.
sourceSets.main.java {
    // No 26.1.2 release: minecolonies, dyenamics
    exclude 'cy/jdkdigital/productivebees/compat/minecolonies/**'
    exclude 'cy/jdkdigital/productivebees/compat/dyenamics/**'
}

// Sets up a dependency configuration called 'localRuntime'.
// This configuration should be used instead of 'runtimeOnly' to declare
// a dependency that will be present for runtime testing but that is
// "optional", meaning it will not be pulled by dependents of this mod.
configurations {
    runtimeClasspath.extendsFrom localRuntime
}

dependencies {
    implementation "local.libs:productivelib-${lib_version}"
    implementation "local.libs:geckolib-neoforge-${minecraft_version}-${geckolib_version}"
    implementation "local.libs:productivemetalworks-26.1.2-1.16.4"
    implementation "curse.maven:glodium-957920:8196173"
    implementation "curse.maven:ex-pattern-provider-892005:8237330"
    implementation "curse.maven:applied-flux-965012:8246560"

    jarJar(group: 'cy.jdkdigital.productivelib', name: 'productivelib', version: "${lib_version_range}") {
        transitive(false)
    }
//    jarJar(group: 'software.bernie.geckolib', name: 'geckolib', version: '[neoforge-26.1.2-5.5.1,)') {
//        transitive(false)
//    }

    compileOnly "mezz.jei:jei-${minecraft_version}-neoforge-api:${jei_version}"
    runtimeOnly "mezz.jei:jei-${minecraft_version}-neoforge:${jei_version}"

//    compileOnly "com.almostreliable.mods:almostunified-neoforge:1.21.1-1.0.0:api"
//
//    compileOnly "vazkii.patchouli:Patchouli:${patchouli_version}:api"
//    runtimeOnly "vazkii.patchouli:Patchouli:${patchouli_version}"
//
//    compileOnly(fg.deobf("com.almostreliable.mods:almostunified-forge:${minecraft_version}-${au_version}"))

    // Mod-compat dependencies — file IDs resolved against MC 26.1.2 on CurseForge.
    // Mods without a 26.1.2 release yet remain commented; re-check periodically.
    implementation "curse.maven:jei-238222:8014757"
    implementation "curse.maven:jade-324717:8651070"
    implementation "curse.maven:curios-309927:7925547"
//    implementation "curse.maven:just-enough-archaeology-890755:5518258"  // no 26.1.2
    implementation "curse.maven:cloth-config-348521:7817583"
//    implementation "curse.maven:modern-industrialization-405388:5663677"  // no 26.1.2
    implementation "curse.maven:biomes-o-plenty-220318:7986475"
    implementation "curse.maven:glitchcore-955399:7975606"
    implementation "curse.maven:terrablender-neoforge-940057:8046313"
    implementation "curse.maven:allthemodium-364466:8157737"
    implementation "curse.maven:ato-405593:8023670"
//    implementation "curse.maven:regions-unexplored-659110:5579932"  // no 26.1.2
//    implementation "curse.maven:blueprint-382216:6965808"  // no 26.1.2
//    implementation "curse.maven:gallery-1173553:6449910"  // no 26.1.2
//    implementation "curse.maven:woodworks-543610:6499291"  // no 26.1.2
//    implementation "curse.maven:autumnity-365045:6969592"  // no 26.1.2
//    implementation "curse.maven:upgrade-aquatic-326895:6969604"  // no 26.1.2
//    implementation "curse.maven:atmospheric-362393:6969589"  // no 26.1.2
//    implementation "curse.maven:extreme-reactors-250277:5538095"  // no 26.1.2
//    implementation "curse.maven:zerocore-247921:5464103"  // no 26.1.2
    implementation "curse.maven:ender-io-64578:8192909"
    implementation "curse.maven:ender-io-endergy-304346:8017163"
    implementation "curse.maven:guideme-1173950:8204880"
    implementation "curse.maven:applied-energistics-2-223794:8129795"
    implementation "curse.maven:aquaculture-60028:7901645"
    implementation "curse.maven:oritech-1030830:8678136"
    implementation "curse.maven:athena-841890:7970442"
    // library deps required by the compat mods below
    implementation "curse.maven:cyclops-core-232758:8527468"
    implementation "curse.maven:modonomicon-538392:8594747"
    implementation "curse.maven:cucumber-272335:8433624"
    implementation "curse.maven:evilcraft-74610:8641499"
    // Blood Magic rebranded as Neo Vitae; no 26.1 Blood Magic build exists
    implementation "curse.maven:neovitae-1404763:8690936"
    implementation "curse.maven:ftb-industrial-contraptions-forge-539097:8683232"
    implementation "curse.maven:mystical-agriculture-246640:8602813"
    implementation "curse.maven:mystical-agradditions-256247:8516018"
    implementation "curse.maven:occultism-361026:8615441"
    // Powah's 26.1 line is a separate CurseForge project from the original "powah"
    implementation "curse.maven:powah-rearchitected-633483:8025339"
//    implementation "curse.maven:dyenamics-509276:5788247"  // no 26.1.2
    implementation "curse.maven:forbidden-arcanus-309858:8521347"
    implementation "curse.maven:valhelsia-core-416935:7916512"
//    implementation "curse.maven:immersive-engineering-231951:5828000"  // no 26.1.2
//    implementation "curse.maven:the-bumblezone-forge-362479:6585522"  // no 26.1.2
//    implementation "curse.maven:towntalk-900364:5653504"  // no 26.1.2
//    implementation "curse.maven:structurize-298744:6334987"  // no 26.1.2
//    implementation "curse.maven:multi-piston-303278:5783614"  // no 26.1.2
//    implementation "curse.maven:domum-ornamentum-527361:5764083"  // no 26.1.2
//    implementation "curse.maven:blockui-522992:6150484"  // no 26.1.2
//    implementation "curse.maven:minecolonies-245506:6312091"  // no 26.1.2
//    implementation ("com.ldtteam:datagenerators:1.20.4-0.1.57-ALPHA:universal") {
//        transitive = false
//    }
//    implementation "curse.maven:modular-bees-1350380:7273528"  // no 26.1.2
//    implementation "curse.maven:glodium-957920:5821676"  // no 26.1.2
    implementation "curse.maven:kotlin-for-forge-351264:7736203"
}

tasks.withType(ProcessResources).configureEach {
    var replaceProperties = [
            minecraft_version      : minecraft_version,
            minecraft_version_range: minecraft_version_range,
            neo_version            : neo_version,
            neo_version_range      : neo_version_range,
            mod_id                 : mod_id,
            mod_name               : mod_name,
            mod_license            : mod_license,
            mod_version            : mod_version,
            mod_authors            : mod_authors,
            mod_description        : mod_description,
            lib_version            : lib_version,
            lib_version_range      : lib_version_range,
            jei_version            : jei_version,
            geckolib_version       : geckolib_version,
            patchouli_version      : patchouli_version
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/neoforge.mods.toml']) {
        expand replaceProperties
    }
}

// Example configuration to allow publishing using the maven-publish plugin
publishing {
    publications {
        register('mavenJava', MavenPublication) {
            from components.java
        }
    }
    repositories {
        maven {
            url "file://${project.projectDir}/repo"
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8' // Use the UTF-8 charset for Java compilation
}

// IDEA no longer automatically downloads sources/javadoc jars for dependencies, so we need to explicitly enable the behavior.
idea {
    module {
        downloadSources = true
        downloadJavadoc = true
    }
}
